# Qdrant config — place next to qdrant.exe
# Data stored at <project_root>/databases/qdrant/

storage:
  storage_path: ./databases/qdrant

service:
  host: 0.0.0.0
  http_port: 6333
  grpc_port: 6334

gpu:
  indexing:
    enabled: true   # AMD RX 6700 XT via Vulkan
