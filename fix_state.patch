--- src/state.rs	2026-03-09 10:59:00.000000000 -0500
+++ src/state.rs	2026-03-09 10:59:00.000000000 -0500
@@ -192,6 +192,8 @@
     // Output visualization frames (512x256 typically)
     // Modified by sync loop, read by cpal callback
     pub output_frames: Mutex<VecDeque<Vec<f32>>>,
+
+    pub wavefield_image: Mutex<Vec<u8>>,

     // UI telemetry messages
     // Channel updated from async threads, polled by UI timer
@@ -321,6 +323,7 @@
             memo_count: AtomicU32::new(0),
             mamba_latency_ms: AtomicF32::new(0.0),
             mamba_anomaly_score: AtomicF32::new(0.0),
+            wavefield_image: Mutex::new(vec![0; 1024 * 1024 * 4]),
             impulse_latency_ms: AtomicF32::new(0.0),
             impulse_anomaly_score: AtomicF32::new(0.0),
             harassment_detected: AtomicBool::new(false),
